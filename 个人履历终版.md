# 崔亦哲

17794230123 | 深圳 | 1996.11 | 兰州大学 · 计算机科学与技术（本科，2019 年毕业）
7 年研发经验 | 求职意向：AI / AI Agent / 代码智能方向
期望薪资：30K/月（税前）| 目标城市：成都 / 杭州 / 上海

---

## 个人总结

- 7 年研发，成长路径：Java 后端 → NLP/LLM 算法工程化 → 代码智能 & AI Agent 工具链
- 2023 年即入局 LLM：BERT 微调、弱监督标注、LLM 规则重排、ICL + DPO 训练（测试集 99%），并主导 ChatGLM 等大模型在垂直领域的可行性评估
- 近两年主导 AI Agent 工具链建设：四语言代码图谱引擎（Python → Go 重写）、MCP 服务、RAG 故障诊断平台，端到端交付 5 个代码仓
- 双重 Agent 经验：**既造 Agent 消费的基础设施（MCP + 代码图谱 + RAG），也用 Agent 高速交付工程**——以 Claude Code / Codex / opencode 驱动研发，1 个月完成四语言引擎 Go 重写（134 commits）

---

## 工作经历

### 华为（OD） | 研发工程师 | 2021.07 – 至今

#### 2025 – 至今 · 代码智能与 AI Agent 工具链（独立负责）

**项目一：代码图谱静态分析引擎 + MCP 服务（Python → Go 双实现）**

- 构建基于 Clang AST（C/C++）、AST + Jedi（Python）、LSP（Java）的多语言静态解析引擎，抽取函数调用链、模块依赖、错误日志定位元数据，输出面向 AI Agent 的代码图谱
- 设计 MCP（Model Context Protocol）服务，为 AI Agent 提供标准化代码图谱查询接口（search_code / get_file_dependencies / search_error_log / list_external_calls 等 9 个工具），使大模型工具调用具备"按代码事实回答"的落地能力
- 用 Go 重写引擎：基于 tree-sitter + LSP 支持 C/C++/Java/Python 四语言；设计 LangSpec 框架（语言无关遍历器 + 语言特化规格）、Graph Buffer（跨文件节点合并、确定性去重）、CBM 置信链 / MRO 方法解析，四语言语义与 Python 基线逐字段对齐（验收 harness 73/73 通过）
- 以 AI Agent 驱动研发流程：1 个月从 initial commit 交付生产级四语言引擎（134 commits / 132 issues）；一次性建成 5 个代码仓的 CI/CD（双平台构建矩阵、pre-push 门禁、PyInstaller 打包）
- 架构质量：深层模块化重构（port 抽象、server 瘦身至 ~100 行）、101+ 单元测试、624+ 测试用例、coverage 地板 82%
- 配套数据管道：从 0 到 1 建设 CMC 数据同步平台（全链路异步化、SQLite→PostgreSQL 迁移 + Alembic、ETL 增量同步），同步耗时 -54.5%、回拉接口 -45.9%

**项目二：故障诊断工具 FaultPro（RAG + eBPF + 根因分析）**

- 设计 RAG 故障知识库：数据清洗 → 语义向量检索 → 精排全链路
- 开发 eBPF 慢 IO 定界工具：无侵入式 IO 路径追踪，VFS → IO 调度器 → NFS 网络时延分段统计，采集开销 < 2%
- 收敛 RTOS / 数据库 / 内核多格式日志为统一解析适配器体系（ASan 调用栈提取、GCC 后缀函数名、pc:/lr: 栈帧识别）
- 打通"故障日志 → 代码图谱匹配 → 故障传播链 → 根因排序"全链路
- 基于 opencode 构建批量诊断 Agent 工作流（超时续跑、部分输出复用），集成国产 AI 算力诊断工具实现"日志诊断 → 故障链建立 → 知识库沉淀"闭环

#### 2023 – 2024 · NLP/LLM 算法工程化（多厂商配置翻译系统）

- 算法研发：配置模板匹配（长文本 + BERT，top5 准确率 95%）、命令参数匹配（Snorkel 弱监督 + BERT，top10 75%，缓存 + numba 加速后耗时 -95%）
- LLM 增强：LLM 规则优选重排（top3 提升至 93%）；基于通义千问的配置生成（ICL 优化 + DPO 微调），测试集准确率 99%
- 大模型调研：设计评测场景，评估 ChatGLM / 盘古在垂直业务领域的辅助能力，产出团队 LLM 落地建议
- 平台交付：配置翻译后台（规则推荐 / 规则库 / 源配置翻译）；独立搭建 GPU 训练环境支撑 BERT 微调、UIE 实体抽取

#### 2021.07 – 2022.12 · Java 后端开发

- 认知闭环微服务研发，独立支撑功能测试 → 开发 → 上线全流程，重构后模块接口简化 50%
- 数字地图平台后台开发，基于 Docker 部署，支撑运营商联创样机验收

### 浪潮集团 | Java 后端开发 | 2019.07 – 2021.07

- 医疗健康体检系统后端开发，负责体检流程、预约、报告查询等模块的设计与上线

---

## 技术栈

| 层面 | 技术 |
|------|------|
| 语言 | Python（主力）、Go（主力）、Java、C/C++（解析目标） |
| AI / LLM | MCP 服务设计、RAG（向量检索 + 精排）、LLM 微调（DPO / ICL / 重排）、BERT、弱监督（Snorkel）、大模型评估 |
| Agent 工程 | Claude Code / Codex / opencode 驱动研发、Agent 批量任务工作流（超时续跑 / 结果复用） |
| 代码分析 | Clang AST、tree-sitter、LSP、Jedi、CBM 置信链、MRO |
| 系统 / 数据 | eBPF、PostgreSQL（Alembic / 分区表）、ETL、异步化、CI/CD 双平台、PyInstaller |
